package appcloud.api.enums;

public enum AttachStatusEnum {
	ATTACHED, DETACHED;
    public String toString() {
        switch (this) {
        case ATTACHED:
            return "ATTACHED";
        case DETACHED:
            return "DETACHED";
        }
        return super.toString();
    }
}
