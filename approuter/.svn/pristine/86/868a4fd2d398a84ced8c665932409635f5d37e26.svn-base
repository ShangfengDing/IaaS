package appcloud.approuter.tip;

import appcloud.common.proxy.DomainProxy;
import appcloud.common.proxy.NginxProxy;
import appcloud.common.proxy.RoutingEntryProxy;
//import appcloud.dbproxy.simple.SimpleDomainProxy;
//import appcloud.dbproxy.simple.SimpleNginxProxy;
//import appcloud.dbproxy.simple.SimpleRoutingEntryProxy;
import appcloud.rpc.tip.TipRPCServer;

public class ProxyServer {
	private static TipRPCServer server = null;
	
	public static void main(String[] args) throws Exception {
		
//		server = new TipRPCServer("ProxyServer", DomainProxy.class, new SimpleDomainProxy());
////		server.addService(DomainProxy.class, new SimpleDomainProxy());
//		server.addService(RoutingEntryProxy.class, new SimpleRoutingEntryProxy());
//		server.addService(NginxProxy.class, new SimpleNginxProxy());
//		server.mainLoop();
	}
}
