package appcloud.vmcontroller;

import static org.junit.Assert.*;

import org.junit.Test;

public class VMControllerConfTest {

    @Test
    public void test() {
       assertEquals(VMControllerConf.VIR_CON_STR, "qemu:///system");
       assertEquals(VMControllerConf.PRIVATE_NETWORK, "br0");
       assertEquals(VMControllerConf.PUBLIC_NETWORK, "br1");
       assertEquals(VMControllerConf.DEFAULT_NETWORK, "bridge");
       assertEquals(VMControllerConf.LIBVIRT_TYPE, "hvm");
       assertEquals(VMControllerConf.POER_22_ACTION, "accept");
       assertEquals(VMControllerConf.FILTER_REF_NAME, "clean-traffic");
       assertEquals(VMControllerConf.MONITER_INTERVAL, 30000);
    
    }

}
